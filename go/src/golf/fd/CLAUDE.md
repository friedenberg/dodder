# fd

File descriptor abstraction with state tracking.

## Key Types

- `FD`: File descriptor with path, mod time, digest, and state
- `State`: File state enum

## Features

- Stdin detection
- File state management
- Standard file descriptors (stdin, stdout, stderr)
