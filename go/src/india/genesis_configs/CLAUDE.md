# genesis_configs

Genesis configuration interfaces for repository initialization.

## Key Interfaces

- `Config`: Base genesis config (version, keys, repo ID)
- `ConfigPublic`: Public genesis configuration
- `ConfigPrivate`: Private genesis with secret key
- `ConfigPrivateMutable`: Mutable private config
