# checkout_mode

Bitflag enum for checkout operation modes.

## Modes

- `Metadata`: Checkout metadata only
- `Blob`: Checkout blob content
- `Lockfile`: Include lockfile
- `Conflict`: Include conflict markers
- `MetadataAndBlob`: Common combination
- `All`: All modes enabled
