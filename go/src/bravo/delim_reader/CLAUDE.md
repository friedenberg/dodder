# delim_reader

Delimited reader for parsing segmented input streams.

## Key Types

- `DelimReader`: Interface for reading delimited segments
- Pooled implementation for memory efficiency

## Features

- Read strings and key-value pairs
- Track segment count and EOF state
