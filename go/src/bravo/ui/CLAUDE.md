# ui

Logging and output printing utilities for CLI user interface.

## Key Types

- `Printer`: Interface for formatted output
- `DevPrinter`: Extended printer with caller info and stack traces

## Key Functions

- `SetVerbose`: Enable/disable verbose logging
- `SetTesting`: Configure for test mode
- `Log`, `Debug`, `Out`, `Err`: Printers for different output streams
