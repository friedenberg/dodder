# options_tools

CLI options for external tool integration.

## Key Types

- `Options`: Configuration for merge tools and other external utilities

## Features

- Merge tool configuration via `--merge-tool` flag
- Shell-style argument parsing with shlex
