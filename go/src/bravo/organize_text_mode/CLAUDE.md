# organize_text_mode

Enum for text organization operation modes.

## Modes

- `ModeInteractive`: User interaction required
- `ModeCommitDirectly`: Apply changes without confirmation
- `ModeOutputOnly`: Display changes without applying
