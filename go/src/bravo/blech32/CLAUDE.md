# blech32

Modified bech32 encoding using `-` as separator instead of `1`.

Based on BIP173 reference implementation, adapted for Dodder's identifier format.

## Key Functions

- Encode/decode human-readable identifiers
- Checksum validation
