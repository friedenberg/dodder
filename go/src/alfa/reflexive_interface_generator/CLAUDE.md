# reflexive_interface_generator

Code generation package (no Go source files).

## Notes

- Configuration only package (contains .claude settings)
- Likely used for interface generation workflows
- No runtime code present
