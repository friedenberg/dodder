# compression_type

Compression type enum with reader/writer factories.

## Supported Types

- `None`: No compression
- `Gzip`: Gzip compression
- `Zlib`: Zlib compression
- `Zstd`: Zstandard compression (default)

## Features

- Reader/writer creation for each type
- CLI flag integration
