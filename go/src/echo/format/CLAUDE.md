# format

Format writer utilities for type-safe I/O.

## Key Functions

- `MakeWriter`: Create writer from format function and element
- `MakeWriterOr`: Write first non-empty element
- `MakeWriterPtr`: Writer for pointer types
