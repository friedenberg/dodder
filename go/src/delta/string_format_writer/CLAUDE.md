# string_format_writer

Formatted string output with color and styling support.

## Key Types

- `Box`: Container for formatted fields
- `Field`: Individual formatted field
- `ColorType`: ANSI color type constants

## Features

- Status strings (new, changed, deleted, etc.)
- Color coding for different element types
- Box-based layout formatting
