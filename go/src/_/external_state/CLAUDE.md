# external_state

State enumeration for external object tracking.

## Type

- `State`: Enumeration representing object tracking status

## Constants

- `Unknown`: Default/uninitialized state
- `Tracked`: Object is actively tracked
- `Untracked`: Object exists but is not tracked
- `Recognized`: Object is known but not fully tracked

Used to represent the relationship between objects and the version control system.
