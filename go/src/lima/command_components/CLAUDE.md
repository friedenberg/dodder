# command_components

Environment factory for standard Dodder commands.

## Purpose

Creates configured environment instances for command execution.

## Key Types

- `Env`: Factory for creating command environments

## Features

- Creates standard local environments with UI and directory layout
- Supports custom XDG dotenv paths for alternative configurations
- Integrates UI options and debug settings from command requests
