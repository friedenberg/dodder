# checkout_options

Checkout configuration options for object sync operations.

## Key Types

- `Options`: Full checkout options with mode and flags
- `OptionsWithoutMode`: Options excluding checkout mode (force, allow conflicted)

## Features

- Checkout mode flag integration
- Force update option to overwrite existing checkouts
- Store-specific options passthrough
